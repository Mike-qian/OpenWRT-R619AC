name: Build-R619ac-24.10

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'false'
  schedule:
        - cron: 0 5 * * *

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt.git
  REPO_BRANCH: openwrt-24.10
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: 24.10.config
  DIY_P1_SH: diy5.sh
  DIY_P2_SH: diy-part2.sh
  UPLOAD_BIN_DIR: true
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai
  GITHUB_TOKEN: ${{ secrets.TOKEN }}
  # 优化编译环境变量
  CCACHE_DIR: /github/home/.ccache
  CCACHE_MAXSIZE: 10G
  CCACHE_COMPRESS: 1
  CCACHE_COMPRESSLEVEL: 8
  CCACHE_SLOPPINESS: time_macros,include_file_mtime,include_file_ctime,file_macro
  CCACHE_HARDLINK: 1
  # 优化下载环境变量
  GIT_SSL_NO_VERIFY: 1
  # 优化编译变量
  MAKEFLAGS: -j$(nproc) --output-sync=target
  GCC_COLORS: auto
  # 优化链接器
  LD: lld
  LDFLAGS: -fuse-ld=lld

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: 准备完成
      uses: actions/checkout@main

    - name: 检查服务器配置
      run: |
        echo "若分配的服务器性能不足，务必及时取消，重新运行！"
        echo -e "------------------------------- CPU信息 -------------------------------\n"
        echo "CPU物理数量:$(cat /proc/cpuinfo| grep "physical id"| sort| uniq| wc -l)"
        echo -e "CPU核心及版本信息: $(cat /proc/cpuinfo | grep name | cut -f2 -d: | uniq -c) \n"
        echo -e "------------------------------- 内存信息 -------------------------------\n"
        echo "已安装内存详细信息: "
        sudo lshw -short -C memory | grep GiB
        echo -e "\n"
        echo -e "------------------------------- 磁盘信息 -------------------------------\n"
        echo -e "磁盘数量: $(ls /dev/sd* | grep -v [1-9] | wc -l) \n"
        echo "------------------------------- 磁盘详情 -------------------------------\n"
        df -Th

    - name: Maximize build space
      uses: easimon/maximize-build-space@master
      with:
        swap-size-mb: 4096
        temp-reserve-mb: 1024
        root-reserve-mb: 8192
        remove-dotnet: 'true'
        remove-android: 'true'
        remove-haskell: 'true'
        remove-codeql: 'true'

    - name: Checkout
      uses: actions/checkout@v5

    - name: 安装 Go 1.21.1
      uses: actions/setup-go@v5
      with:
        go-version: 'stable'

    - name: 初始化编译环境
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        # 清理不需要的文件和目录
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        docker rmi $(docker images -q) 2>/dev/null || true
        # 更换为更快的APT源
        sudo sed -i 's/archive.ubuntu.com/mirrors.aliyun.com/g' /etc/apt/sources.list
        sudo sed -i 's/security.ubuntu.com/mirrors.aliyun.com/g' /etc/apt/sources.list
        # 优化APT更新和安装
        sudo -E apt-get -qq update -y
        sudo -E apt-get -qq install -y --no-install-recommends ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
        bzip2 ccache clang cmake cpio curl device-tree-compiler ecj fastjar flex gawk gettext gcc-multilib \
        g++-multilib git gnutls-dev gperf haveged help2man intltool lib32gcc-s1 libc6-dev-i386 libelf-dev \
        libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses-dev libpython3-dev \
        libreadline-dev libssl-dev libtool libyaml-dev libz-dev lld llvm lrzsz mkisofs msmtp nano \
        ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip python3-ply python3-docutils \
        python3-pyelftools qemu-utils re2c rsync scons squashfs-tools subversion swig texinfo uglifyjs \
        upx-ucl unzip vim wget xmlto xxd zlib1g-dev zstd
        # 清理APT缓存
        sudo -E apt-get -qq autoremove --purge -y
        sudo -E apt-get -qq clean -y
        sudo rm -rf /var/lib/apt/lists/*
        # 设置时区
        sudo timedatectl set-timezone "$TZ"
        # 优化磁盘I/O
        sudo sysctl -w vm.dirty_ratio=10
        sudo sysctl -w vm.dirty_background_ratio=5
        # 启用ccache并设置
        ccache --set-config=max_size=10G
        ccache --set-config=compression=true
        ccache --set-config=compression_level=8
        ccache --set-config=hardlink=true
        ccache --set-config=sloppiness=time_macros,include_file_mtime,include_file_ctime,file_macro
        
    - name: 克隆源码
      run: |
        # 优化git克隆，使用--depth 1减少下载量，添加--single-branch和--no-tags选项
        git clone --depth 1 --single-branch --no-tags $REPO_URL -b $REPO_BRANCH openwrt

    - name: 设置缓存
      uses: actions/cache@v4
      with:
        path: |
          openwrt/dl
          openwrt/build_dir
          openwrt/staging_dir
          openwrt/feeds
          openwrt/.config
          ~/.ccache
          ~/.cache
          /tmp/dl*
        key: ${{ runner.os }}-openwrt-${{ env.REPO_BRANCH }}-${{ hashFiles('${{ env.DIY_P1_SH }}', '${{ env.DIY_P2_SH }}', '${{ env.CONFIG_FILE }}') }}-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-openwrt-${{ env.REPO_BRANCH }}-${{ hashFiles('${{ env.DIY_P1_SH }}', '${{ env.DIY_P2_SH }}', '${{ env.CONFIG_FILE }}') }}-
          ${{ runner.os }}-openwrt-${{ env.REPO_BRANCH }}-

    - name: 加载自定义 feeds
      run: |
        [ -e $FEEDS_CONF ] && mv $FEEDS_CONF openwrt/feeds.conf
        chmod +x $DIY_P1_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P1_SH

    - name: 更新 feeds
      run:  |
          cd openwrt
          # 优化feeds更新，使用-j选项并行更新
          ./scripts/feeds update -a -j$(nproc)
          
    - name: 安装 feeds
      run: |
          cd openwrt
          rm -rf feeds/packages/lang/golang
          git clone https://github.com/sbwml/packages_lang_golang.git feeds/packages/lang/golang
          # 优化feeds安装，使用-j选项并行安装
          ./scripts/feeds install -a -j$(nproc)
          ./scripts/feeds uninstall {mmdvm,mmdvm-host,p25-clients,ysf-clients,nxdn-clients,mmdvm-luci,dapnet-gateway,ircddb-gateway,libmmdvm,libwxwidgets,mmdvm-cal}
 
    - name: 加载自定义配置
      run: |
        [ -e files ] && mv files openwrt/files
        [ -e $CONFIG_FILE ] && mv $CONFIG_FILE openwrt/.config
        chmod +x $DIY_P2_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: SSH 连接到 Actions
      uses: P3TERX/ssh2actions@v2
      if: (github.event.inputs.ssh == 'true' && github.event.inputs.ssh  != 'false') || contains(github.event.action, 'ssh')
      env:
        GITHUB_TOKEN: ${{ secrets.TOKEN }}

    - name: 下载软件包
      id: package
      run: |
        cd openwrt
        make defconfig
        # 增加下载并行度
        make download -j$(nproc)
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: 编译固件
      id: compile
      run: |
        cd openwrt
        echo -e "$(nproc) thread compile"
        # 启用ccache加速编译
        echo 'CONFIG_CCACHE=y' >> .config
        echo 'CONFIG_CCACHE_DIR="/github/home/.ccache"' >> .config
        echo 'CONFIG_CCACHE_COMPRESS=y' >> .config
        echo 'CONFIG_CCACHE_COMPRESSLEVEL=8' >> .config
        echo 'CONFIG_CCACHE_MAXSIZE="10G"' >> .config
        echo 'CONFIG_CCACHE_HARDLINK=y' >> .config
        echo 'CONFIG_CCACHE_SLOPPINESS="time_macros,include_file_mtime,include_file_ctime,file_macro"' >> .config
        # 优化编译选项
        echo 'CONFIG_GCC_OPTIMIZE_FOR_PERFORMANCE=y' >> .config
        echo 'CONFIG_GCC_USE_GRAPHITE=y' >> .config
        echo 'CONFIG_PKG_ASLR_PIE_ALL=y' >> .config
        # 设置编译并行度为CPU核心数+1，提高编译效率
        CORES=$(($(nproc) + 1))
        echo "Using $CORES threads for compilation"
        make defconfig
        # 启用ccache统计
        ccache -s
        # 开始编译
        make -j$CORES CCACHE_DIR=/github/home/.ccache || make -j1 CCACHE_DIR=/github/home/.ccache || make -j1 V=s CCACHE_DIR=/github/home/.ccache
        # 显示ccache统计信息
        ccache -s
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        echo "status=success" >> ${GITHUB_OUTPUT}

    - name: 检查磁盘空间
      if: (!cancelled())
      run: df -hT

    - name: 上传 bin 文件夹
      uses: actions/upload-artifact@main
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      with:
        name: OpenWrt_bin${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: openwrt/bin

    - name: 整理固件文件
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        cp -f ../../../../.config ./
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> ${GITHUB_OUTPUT}

    - name: 上传固件目录
      uses: actions/upload-artifact@main
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE }}

    - name: 生成发布标签
      id: tag
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_RELEASE == 'true' && !cancelled()
      run: |
        echo "release_tag=$(date +"%Y.%m.%d-%H.%M")" >> ${GITHUB_OUTPUT}
        touch release.txt
        echo "
        💻 路由: P&W R619AC 128M Immortalwrt

        📂 源码: ${{ env.REPO_URL }}

        🌳 分支: ${{ env.REPO_BRANCH }}

        ⏱️ 编译时间: $(date +"%Y年%m月%d日%H时%M分")

        🌐 管理地址: 192.168.1.1

        👤 用户名: root

        🔒 密码: 无密码 

        🚧 软件包-配置opkg-更新以下源
        src/gz openwrt_kiddin9 https://dl.openwrt.ai/latest/packages/arm_cortex-a7_neon-vfpv4/kiddin9
        
        " >> release.txt
        echo "status=success" >> ${GITHUB_OUTPUT}

    - name: 自动发布固件到 Releases
      uses: softprops/action-gh-release@v2
      if: steps.tag.outputs.status == 'success' && !cancelled()
      env:
        GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}
      with:
        tag_name: ${{ steps.tag.outputs.release_tag }}
        body_path: release.txt
        files: ${{ env.FIRMWARE }}/*

    - name: 删除运行记录
      uses: Mattraks/delete-workflow-runs@main
      with:
        retain_days: 7
        keep_minimum_runs: 7
        token: ${{ env.GITHUB_TOKEN }}

    - name: 删除自动发布的旧固件
      uses: dev-drprasad/delete-older-releases@master
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        keep_latest: 7
        delete_tags: true
